// Platform product name, shown wherever a tenant hasn't set its own
// display name in Branding. Internal identifiers (package names, storage
// keys, the av-bridge binary/service) intentionally keep the old name.
export const PRODUCT_NAME = "M.A.R.C.U.S.";
