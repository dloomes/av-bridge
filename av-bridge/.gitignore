# Compiled binary — leading slash anchors to this file's directory so the
# pattern only matches the binary, not the cmd/av-bridge package directory.
/av-bridge.exe
/av-bridge
/av-bridge-linux-amd64
/av-bridge-linux-arm64
/av-bridge-linuxarm64

# Secrets - never commit these
poc.env
*.env
/etc/av-bridge/

# State store
*.json
!go.sum

# Test coverage
coverage.out
coverage.html

# IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db
